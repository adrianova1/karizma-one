# سند معماری و مشخصات فنی مربی کاریزما (Karizma Coach Architecture Manifest)

> **قانون اساسی مربی کاریزما:** مربی کاریزما (Karizma Coach) یک سیستم بازیابی قطعی، بومی (Local Non-AI Retrieval)، سریع (زیر ۱۵۰ میلی‌ثانیه) و ایزوله با **۶۰,۰۰۰ سناریوی کانونیکال** است. سیستم به مدل‌های زبانی خارجی (LLM) یا توهمات متنی وابسته نبوده و ۵ پاسخ استاندارد را با بالاترین دقت از بانک کانونیکال استخراج می‌کند.

---

## ۱. نقشه داده‌های مخزن و تفکیک نقش‌ها (Repository Data Map)

برای جلوگیری از هرگونه تداخل معماری (Data & Contract Drift)، منابع داده به صورت شفاف دسته‌بندی و تعیین نقش شده‌اند:

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                          KARIZMA REPOSITORY DATA MAP                        │
├──────────────────────────────┬─────────────────────────┬────────────────────┤
│ مسیر فایل / دایرکتوری         │ نقش در سیستم             │ وضعیت اجرا در رانتایم │
├──────────────────────────────┼─────────────────────────┼────────────────────┤
│ data/chunks/ (001 تا 012)     │ بانک ۶۰K کانونیکال مربی │ ✅ هسته اصلی مربی   │
│ data/chunks/manifest.json    │ مانیفست نسخه 2.0.0      │ ✅ مرجع بارگذاری    │
│ src/data/scenarios.ts        │ کارت‌های لایتنر (۵۰ عدد)│ ✅ بخش آموزش تعاملی │
│ Scenario_Bank.txt            │ کورپوس متنی اولیه       │ 📦 مرجع سورس آفلاین │
│ data/coach/benchmark.json    │ مجموعه آزمون‌های بنچ‌مارک│ 🧪 سنجش کیفیت و دقت │
│ data/scenarios.json          │ نسخه بک‌آپ ساختاری       │ 📦 مرجع مقایسه‌ای   │
└──────────────────────────────┴─────────────────────────┴────────────────────┘
```

---

## ۲. خط لوله پردازش قطعی مربی کاریزما (Deterministic Coach Pipeline)

```text
                     پرسش کاربر (User Query)
                               │
                               ▼
               PersianNormalizer (نرمال‌سازی پیشرفته)
    ├─ اصلاح ی/ک، نیم‌فاصله‌ها و حذف علائم نگارشی اضافه
    ├─ تفکیک توکن‌ها و واژه‌سازی N-Gram
                               │
                               ▼
                CoachIndex (ایندکس حافظه ۶۰ هزارتایی)
    ├─ تطابق دقیق تریگر (Exact Trigger Matching) - امتیاز 100
    ├─ شمول عبارات و الگوها (Phrase Containment) - امتیاز 85
    ├─ شباهت ترای‌گرام نام‌های مستعار (Trigram Similarity) - امتیاز 70
    └─ وزن‌دهی توکن‌های پرتکرار با BM25 - امتیاز بر اساس تراکم
                               │
                               ▼
             RankingEngine (رتبه‌بندی و امتیاز غنا)
    ├─ محاسبه غنای پاسخ‌ها (Richness Score)
    ├─ محاسبه جریمه تطابق تصادفی (Short/Generic Penalty)
    └─ انتخاب قطعی سناریوی برتر (Best Scenario Selection)
                               │
                               ▼
              ToneDistributor (توزیع ایزوله ۵ لحن)
    ├─ استخراج ۱-به-۱ پنج لحن از فیلد responses همان سناریو
    ├─ ایزولاسیون ۱۰۰٪ بدون نشت سناریویی (CROSS_SCENARIO_RESPONSE = false)
                               │
                               ▼
             ResponseSelector (بسته‌بندی شناسنامه‌دار)
    ├─ الصاق شناسنامه مبدأ سناریو و چانک (Provenance & Audit Log)
    ├─ تولید خروجی ساخت‌یافته JSON و مارک‌داون یکپارچه
                               │
                               ▼
                تحویل خروجی زیر ۱۵۰ میلی‌ثانیه به کاربر
```

---

## ۳. قرارداد ۵ لحن کانونیکال (The 5 Canonical Tones Contract)

تمام لایه‌های Backend، موتور CoachEngine و رابط کاربری Frontend به صورت یکپارچه بر اساس ۵ شناسه کانونیکال زیر عمل می‌کنند:

| شناسه لحن (Tone ID) | آیکون و عنوان فارسی | کارکرد و هدف روانشناختی |
| :--- | :--- | :--- |
| `charismatic` | 👑 **لحن ۱: کاریزماتیک و باکلاس** | پرستیژ کلامی، جذابیت سنجیده و استقلال عاطفی بدون نیازمندی |
| `funny` | 😂 **لحن ۲: شوخ‌طبع و رندانه** | شکستن یخ مکالمه، بازیگوشی هوشمندانه و تلطیف فضا |
| `confident` | 🔥 **لحن ۳: مقتدر و با اعتماد به نفس** | صراحت لهجه، هدایت مکالمه و تعیین مرزهای شفاف و قاطع |
| `mysterious` | 🔮 **لحن ۴: مرموز و پرکشش** | برانگیختن کنجکاوی و ایجاد کشش عاطفی بدون افشای زودهنگام |
| `mature` | 💎 **لحن ۵: متین و پخته** | پاسخ دیپلماتیک، باوقار، عمیق و حفظ احترام و ارزش ارتباط |

---

## ۴. مشخصات فنی و شاخص‌های سلامت سیستم (System Invariants)

1. **تعداد سناریوهای کانونیکال (CANONICAL_SCENARIO_COUNT):** دقیقاً `60,000` سناریو از ۱۲ چانک مجزا.
2. **عدم نشت داده‌های قدیمی (LEGACY_DATASET_USED_BY_COACH):** مقدار `false`؛ موتور به هیچ‌وجه از کورپوس قدیمی ۹K استفاده نمی‌کند.
3. **ایزولاسیون کامل سناریویی (CROSS_SCENARIO_RESPONSE):** مقدار `false`؛ هر ۵ لحن پاسخ دقیقاً متعلق به همان سناریوی انتخاب‌شده هستند.
4. **عملکرد و سرعت (Performance & Latency):**
   - میانگین زمان پاسخگویی محلی: **کمتر از ۱۵۰ میلی‌ثانیه**
   - زمان بالا آمدن اولیه (In-Memory Indexing): **کمتر از ۱۲ ثانیه برای ۶۰K سناریو**
   - دقت تست‌های بنچ‌مارک استاندارد: **بالاتر از ۸۸٪**
